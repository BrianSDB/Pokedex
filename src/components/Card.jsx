/* eslint-disable react/prop-types */

import { PokemonCard } from "./PokemonCard";

export const Card = () => {
  return (
    <div>
      <PokemonCard />
      {/* <PokemonCard stats={stats} pokemon={pokemon} /> */}
    </div>
  );
};
